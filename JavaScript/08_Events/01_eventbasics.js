//All events in javascript run sequentially. there  are some exception available as well but in general all events run sequentially. js sequentially run language. 
// All these browser events will be invoked while having an activity. like:- moving cursor, clicking on button , hovering on elements etc...
//browser events and environment event.

//Have to study about :- 
// type, timestamp, defaultPrevented
// target, toElement, srcElement, currentTarget, 
// clientX, clientY, screenX, screenY, 
// altkey, crtlkey,  shiftkey, keyCode